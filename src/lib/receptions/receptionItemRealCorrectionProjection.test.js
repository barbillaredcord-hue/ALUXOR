import { describe, expect, it } from 'vitest';
import { projectEffectiveReceptionItem } from './receptionItemRealCorrectionProjection.js';
const id=(n)=>`00000000-0000-4000-8000-${String(n).repeat(12)}`;
const item={id:id('1'),workspaceId:id('2'),receptionId:id('3'),purchaseId:id('4'),purchaseItemId:id('5'),receivedQuantity:7,acceptedQuantity:7,actualUnitCost:10,version:1};
const correction={id:id('6'),workspaceId:id('2'),receptionId:id('3'),receptionItemId:id('1'),purchaseId:id('4'),purchaseItemId:id('5'),createdBy:id('7'),idempotencyKey:id('8'),correctionType:'RETURNED_TO_SUPPLIER',status:'active',version:2,reason:'Devuelto',previousValues:{},newValues:{receivedQuantity:7,acceptedQuantity:5},occurredAt:'2026-08-06T00:00:00Z'};
describe('effective reception item projection',()=>{it('conserva original y aplica solo la partida relacionada',()=>{expect(projectEffectiveReceptionItem({receptionItem:item}).acceptedQuantity).toBe(7);expect(projectEffectiveReceptionItem({receptionItem:item,corrections:[correction]}).acceptedQuantity).toBe(5)});it('reversión append-only restaura el original',()=>{const reversal={...correction,id:id('9'),correctionType:'CORRECTION_REVERSAL',reversalOfId:correction.id,version:3,newValues:{},occurredAt:'2026-08-07T00:00:00Z'};expect(projectEffectiveReceptionItem({receptionItem:item,corrections:[correction,reversal]}).acceptedQuantity).toBe(7)})});
